environment:
  matrix:
    - nodejs_version: "6"
platform:
  - x64
install:
  - ps: Install-Product node $env:nodejs_version
  - npm install -g npm
  - npm install -g codecov
  - npm install
test_script:
  - cmd: npm test
after_build:
  - cmd: codecov
cache:
  - node_modules -> package.json    